# Repository Agent Instructions

When editing this repository, treat this as the only operational source:

- `skills/labview-to-python-industrial/SKILL.md`

The rule and translations are derived distribution summaries. Update them when
the skill meaning changes. Run `python scripts/validate_repo.py` before committing.
