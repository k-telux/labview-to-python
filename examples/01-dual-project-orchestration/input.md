# Original Input (Sanitized)

Act as the supervisor for two LabVIEW pump-probe migrations: a regular delay map
and a power-dependent nested map. Create separate scoped execution tasks. Each
project must provide an industrial dark UI and a LabVIEW front-panel replica, but
both UIs must share one workflow, hardware, simulation, and data-processing core.

P0 is exact measurement and processing behavior: scan loops, hardware call order,
unit conversions, wavelength cuts, averaging, map assembly, save schema, and
fail-closed shutdown. Hardware is unavailable, so run deterministic simulated
drivers through the production workflow and compare outputs with supplied data
sheets. Inventory each vendor SDK with source, purpose, hash, license, and blocker.

Do not accept worker self-reports. Review logic/data and screenshots independently.
Do not package until the source gate is explicitly approved. Report simulator,
source UI, packaged EXE, real hardware, and unresolved phase/chopper status
separately.
