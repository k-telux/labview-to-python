# Original Input (Sanitized)

The new application scans much more slowly than the accepted version, and the
power-dependent heatmap no longer matches the original data resolution. Compare
the accepted gate, current source, and target CSV.

Do not gain speed by reducing the 50 power rows, 5 delays, 1024 wavelengths,
repeats, raw saves, or event semantics. Record core and click-to-Completed time,
UI overhead, render counts, semantic events, physical writes, source/displayed
shape, selected delay, and first/middle/last hover coordinates. The quick profile
must be labeled `quick_preview`.

Use real mouse movement. A setter fallback must fail. Compare workflow result,
saved CSV readback, and `ImageItem.image`; do not compare the target CSV with
itself. If the simulator derives inputs from the target, label it as a roundtrip
fixture and keep independent LabVIEW algorithm parity blocked.
