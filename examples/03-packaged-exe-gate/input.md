# Original Input (Sanitized)

The source UI gate is approved. Build a clean windowed one-file EXE from the
current source. Do not reuse the old binary. The root and dist executables must be
the same file by SHA256, and Industrial/LabVIEW shortcuts must have correct
arguments, working directory, and icon.

Prove the build is newer than all relevant product, entry-point, packaging, and
verifier files. Launch the root EXE and both shortcuts. Capture target-window
evidence for driver status, both UIs, quick and full scans, pause/resume, partial
stop, color-table update, reverse control, hover, final saved paths, and output
readback. Keep source, packaged EXE, simulator, and real-hardware verdicts
separate.
